fb(app-id='426661280792851')
  
table.table
  thead
    tr
      th Evento
      th Data
      th Onde
  tbody
    tr(ng-repeat="event in events | orderBy:'start_time':reverse:true")
      td {{event.name}}
      td {{event.start_time | date:'dd/MM/yyyy'}}
      td {{event.location}}

.clearfix
